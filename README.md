# dojot Real-Tim Sample Client

It is a sample code to get device's data from dojot through socket.io.

## Usage

To install dependencies, run:

```shell
npm install
```

To start the client, run:

```shell
node index.js -H <dojot URL> -u <user> -p <password>
```